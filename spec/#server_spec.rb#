require 'fake_smtp'

describe FakeSmtp::Server do

  let(:server) do
    s = FakeSmtp::Server.new(12333)
    Threads.run
    s
  end

  let(:client) {s = TCPSocket.open('localhost', 12333)

  it 'prints the inital greeting' do
    
  end
end